spring:
  datasource:
    url: jdbc:h2:mem:skala_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

skala:
  jwt:
    secret: test-secret-key-for-unit-tests-only-32chars
    expiration-ms: 3600000

logging:
  file:
    name: /tmp/skala-test.log
